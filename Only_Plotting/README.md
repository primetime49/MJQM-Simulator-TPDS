This directory only contains script needed to generate plots/figures with the simulation results already pre-saved in csv format.

To generate the figures, simply run the script: **script-figures.sh**
